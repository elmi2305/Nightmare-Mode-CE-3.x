package com.itlesports.nightmaremode.util.interfaces;

/** Armor that deliberately insulates the wearer instead of inheriting protection from a vanilla material. */
public interface IHeatResistantArmor {
    float getFireTimeReduction();
}
